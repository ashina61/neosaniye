# Remotion fixture

`production.json` is a fixed English collage story used only to validate the generic renderer in CI. It is never uploaded to YouTube and does not modify topic deduplication or publication state.
