# Remotion fixture

`production.json` is a fixed English reel — six voiceover lines, one rig each —
used only to validate the renderer in CI. It carries no media assets on purpose:
if a rig cannot stand up on its coded layers alone, the fixture render shows it.

It is never uploaded to YouTube and does not modify topic deduplication or
publication state.
