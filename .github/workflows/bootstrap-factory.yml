name: Bootstrap NeoSaniye Factory — DISABLED

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live publishing tests are disabled to prevent duplicate uploads."
