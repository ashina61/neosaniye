name: Deprecated Venus Workflow — Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read
  models: read

jobs:
  migration-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the data-driven factory
        run: |
          echo "This legacy workflow no longer renders a topic-specific video."
          echo "Run 'NeoSaniye Vox Factory — Manual Only' and enter the topic there."
