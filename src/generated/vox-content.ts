export type GeneratedVoxCamera = 'slow-push-in' | 'slow-push-out' | 'pan-left' | 'pan-right' | 'drift-up' | 'drift-down' | 'impact-push';
export type GeneratedVoxSfx = 'none' | 'snap' | 'chime' | 'impact';
export type GeneratedVoxScene = {id: string; voiceover: string; headline: string; label?: string; visualPrompt: string; camera: GeneratedVoxCamera; sfx: GeneratedVoxSfx; image: string};
export type GeneratedVoxContent = {slug: string; title: string; language: string; targetDurationSeconds: number; scenes: GeneratedVoxScene[]};

export const VOX_CONTENT: GeneratedVoxContent = {
  slug: 'factory-placeholder',
  title: 'NeoSaniye Factory',
  language: 'en',
  targetDurationSeconds: 42,
  scenes: Array.from({length: 10}, (_, index) => ({
    id: `scene-${String(index + 1).padStart(2, '0')}`,
    voiceover: 'The manual factory workflow replaces this placeholder before rendering.',
    headline: 'FACTORY READY',
    label: 'MANUAL RUN',
    visualPrompt: 'Generated by the factory.',
    camera: index % 2 === 0 ? 'slow-push-in' : 'pan-left',
    sfx: index === 8 ? 'impact' : 'none',
    image: `generated/factory-placeholder/scene-${String(index + 1).padStart(2, '0')}.jpg`,
  })),
};
