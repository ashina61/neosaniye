Drop candidate images here.

Filenames carry no authority — the pipeline scores what is IN the file against
every open brief and reports a ranking, never an assignment. Name them anything.

  npm run assets:match
